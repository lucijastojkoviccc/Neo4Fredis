@{
	ViewData["Title"] = "Chat Page";
}
<form method = "post" asp-controller="Korisnik" asp-action="Logout">
    <button class="btn btn-info">Logout</button>
</form>

<div class="card">
	@(await Html.RenderComponentAsync<Neo4Fredis.Pages.ChatCCC>(RenderMode.Static, new {}))
</div>